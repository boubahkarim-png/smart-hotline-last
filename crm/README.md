```text
This repo contains the static site (index.html + assets) for crm.smart-hotline.com.

To edit:
- Open the file in GitHub, click the pencil icon, edit, and commit to main.
- Netlify will automatically redeploy the site when commits are pushed to the linked branch.

Make sure index.html and any asset folders (images/) are in the repo root.
```
